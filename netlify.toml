[build]
  command = "npm install && npm run build"
  publish = "build"

[functions]
  directory = "functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  MONGO_URI = "your-mongodb-connection-string"
