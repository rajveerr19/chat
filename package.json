{
  "name": "task-management-system",
  "version": "1.0.0",
  "description": "Task Management System with Chat and Video Call Features",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p build && cp -r index.html styles.css chat-styles.css script.js build/",
    "deploy": "npm run build && netlify deploy --prod"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.14.2",
    "mongoose": "^8.0.3",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "netlify-cli": "^12.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
